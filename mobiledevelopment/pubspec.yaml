name: marketing_campaign_mobile
description: Marketing Campaign Management System — mobile app mirroring web src.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  go_router: ^14.6.2
  http: ^1.2.2
  shared_preferences: ^2.3.3
  file_picker: ^8.1.6
  intl: ^0.19.0
  connectivity_plus: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/config/api_base.txt
